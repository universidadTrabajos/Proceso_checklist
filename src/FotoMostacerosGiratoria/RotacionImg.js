import React from 'react';
import './RotatingImage.css'; // Asegúrate de importar el archivo CSS

const RotatingImage = () => {
    return (
        <div className="rotating-image-container">
            <img src="/ProcesologoMosta.webp" alt="logoMosta" className="rotating-image" />
        </div>
    );
};

export default RotatingImage;
